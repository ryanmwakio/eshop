<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="stylesheet" href="/css/styles.css" />
    
    <title><%= title %></title>
    <link rel="shortcut icon" href="/images/logo.png" type="image/x-icon" />
  </head>
  <body>
    <%- include("../includes/navbar") %>
    <main>
      </div class="w-full">
    
        <form class="md:ml-96 mt-10 md:w-1/3 bg-white rounded-lg" method="post" action="/new-password">

          <% if(message){ %>
             <div class="bg-yellow-50 border-l-4 border-yellow-500 text-sm text-yellow-500 px-4 py-2 mx-12" role="alert">
               <p><%= message %>.</p>
            </div>
          <% } %> 

          <input type="hidden" value="<%= userId %>" name="userId">
          <input type="hidden" value="<%= passwordToken %>" name="passwordToken">
          <input type="hidden" value="<%= csrfToken %>" name="_csrf">
        
        <h2 class="text-xl text-center text-purple-700 mb-4">Reset Password</h2>
        <div class="px-12 pb-10">
         
            <div class="w-full mb-2">
                <div class="flex items-center">
                    <i class='ml-3 fill-current text-gray-400 text-xs z-10 fas fa-lock'></i>
                    <input type='password' placeholder="New Password" name="password"
                        class="-mx-6 px-8 w-full border rounded px-3 py-2 text-gray-700 focus:outline-none focus:border-b focus:border-purple-700 focus:shadow-md" required />
                </div>
            </div>
           
            <button type="submit"
                class="w-full py-2 rounded-full bg-purple-600 hover:bg-purple-700 hover:shadow-md text-gray-100  focus:outline-none">Reset Password</button>
        </form>
      <div>
    </main>
  </body>
</html>
