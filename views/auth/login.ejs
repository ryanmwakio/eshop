<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/styles.css" />
    <title><%= title %></title>
  </head>
  <body>
    <%- include("../includes/navbar") %>
    <main>
      </div class="w-full">
    
        <form class="md:ml-96 mt-10 md:w-1/3 bg-white rounded-lg" method="post" action="/login">

          <% if(message){ %>
             <div class="bg-yellow-50 border-l-4 border-yellow-500 text-sm text-yellow-500 px-4 py-2 mx-12" role="alert">
               <p><%= message %>.</p>
            </div>
          <% } %> 

          <input type="hidden" value="<%= csrfToken %>" name="_csrf">
        <div class="flex font-bold justify-center mt-6">
           <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-purple-700" fill="none" viewBox="0 0 24 24" stroke="currentColor">
  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1" />
</svg>
        </div>
        <h2 class="text-xl text-center text-purple-700 mb-4">Login Form</h2>
        <div class="px-12 pb-10">
            <div class="w-full mb-2">
                <div class="flex items-center">
                    <i class='ml-3 fill-current text-gray-400 text-xs z-10 fas fa-user'></i>
                    <input type='email' placeholder="Email" name="email"
                        class="-mx-6 px-8  w-full border rounded px-3 py-2 text-gray-700 focus:outline-none focus:border-b focus:border-purple-700 focus:shadow-md" required />
                </div>
            </div>
            <div class="w-full mb-2">
                <div class="flex items-center">
                    <i class='ml-3 fill-current text-gray-400 text-xs z-10 fas fa-lock'></i>
                    <input type='password' placeholder="Password" name="password"
                        class="-mx-6 px-8 w-full border rounded px-3 py-2 text-gray-700 focus:outline-none focus:border-b focus:border-purple-700 focus:shadow-md" required />
                </div>
            </div>
            <a href="/reset" class="text-xs text-gray-500 float-right mb-4 mr-3">Forgot Password?</a>
            <button type="submit"
                class="w-full py-2 rounded-full bg-purple-600 hover:bg-purple-700 hover:shadow-md text-gray-100  focus:outline-none">sign in</button>
        </form>
      <div>
    </main>
  </body>
</html>
